import type { RemoteMcpConfig } from "./types"

/**
 * Grep.app MCP — GitHub code search.
 * https://mcp.grep.app
 */
export const grepApp: RemoteMcpConfig = {
  type: "remote",
  url: "https://mcp.grep.app",
  enabled: true,
}
